<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>it.uniroma2</groupId>
  <artifactId>tutorlink</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>
  <name>TutorLink</name>
  <description>Tutoring platform - ISPW project, University of Rome Tor Vergata</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>17</maven.compiler.release>
    <javafx.version>17.0.11</javafx.version>
    <junit.version>5.10.2</junit.version>
    <mainClass>it.uniroma2.tutorlink.Main</mainClass>

    <!-- Da compilare con i valori che SonarCloud mostra dopo aver importato la repo. -->
    <sonar.organization>REPLACE-WITH-YOUR-SONAR-ORG</sonar.organization>
    <sonar.projectKey>REPLACE-WITH-YOUR-SONAR-PROJECT-KEY</sonar.projectKey>
    <sonar.host.url>https://sonarcloud.io</sonar.host.url>
    <!-- La parte grafica non entra nell'analisi. -->
    <sonar.exclusions>
      src/main/java/it/uniroma2/tutorlink/boundary/**
    </sonar.exclusions>
    <!-- I tre DAO hanno per forza la stessa forma. -->
    <sonar.cpd.exclusions>
      src/main/java/it/uniroma2/tutorlink/boundary/**,
      src/main/java/it/uniroma2/tutorlink/dao/**
    </sonar.cpd.exclusions>
    <sonar.coverage.jacoco.xmlReportPaths>
      ${project.build.directory}/site/jacoco/jacoco.xml
    </sonar.coverage.jacoco.xmlReportPaths>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>

    <!-- Il driver serve solo a runtime: il codice usa java.sql, non le sue classi. -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>8.3.0</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>

      <!-- Permette di avviare l'applicazione con mvn javafx:run. -->
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>${mainClass}</mainClass>
        </configuration>
      </plugin>

      <!-- Misura la copertura dei test durante la fase verify. -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- Crea il jar eseguibile. La main class e' Launcher e non Main perche' JavaFX
           si rifiuta di partire se la classe di avvio estende Application. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.3</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <!-- Senza questo, shade lascia un dependency-reduced-pom.xml nella
                   radice del progetto: e' un file di appoggio che non ci serve. -->
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>it.uniroma2.tutorlink.Launcher</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
